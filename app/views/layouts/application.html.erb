<!DOCTYPE html>
<html>
  <head>

    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>
  <body>
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-header">
        <%= link_to "Map Votes", root_path, class: "navbar-brand" %>

        <%= simple_form_for :map, url: maps_path, method: "get", html: { class: "navbar-form navbar-left", role: "search"} do |f| %>
          <div class="form-group">
            <%= f.input_field :search, name: "search", class: "form-control" , placeholder: "Search Maps", value: params[:search] %>
          </div>
          <%= f.submit "Search", class: "btn btn-default" %>
        <% end %>

      </div>

      <div class="collapse navbar-collapse">
        <ul class ="nav navbar-nav">
          <li><%= link_to "Map List", maps_path %></li>
          <% if can? :create, Map %>
            <li><%= link_to "Add Map", new_map_path %></li>
          <% end %>
          <% if can? :read, ApiKey %>
            <li><%= link_to "Add API Key", api_keys_path %></li>
          <% end %>
          <% if can? :read, current_user %>
            <li><%= link_to "Account Info", user_path(current_user) %></li>
          <% end %>
        </ul>

        <div class="navbar-header navbar-right">
          <% if current_user %>
            <p class="navbar-text">
              Welcome <%= link_to current_user.nickname, current_user %>
              <%= link_to "Log Out", logout_path %>
              <%= link_to image_tag(current_user.avatar_icon_url, alt: "avatar"), current_user.profile_url %>
            </p>
          <%else %>
            <p class="navbar-text">
              <%= link_to image_tag("sits_small.png", alt: "Sign in with Steam"), "/auth/steam" %>
            </p>
          <% end %>
        </div>

      </div>
    </nav>

    <%= render partial: "shared/flash_messages", flash: flash %>

    <div class="container" >
      <%= yield %>
    </div>

    <footer class="navbar navbar-inverse navbar-fixed-bottom">
      <div class="row">
        <div class="col-lg-12">
          <p><%= link_to "Powered By Steam", "http://steampowered.com" %></p>
        </div>
      </div>
    </footer>

    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
