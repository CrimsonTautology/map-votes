<!DOCTYPE html>
<html>
  <head>

    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>
  <body>
    <header class="navbar navbar-fixed-top navbar-inverse">
    <div class="navbar-inner">
      <div class="container">
        <%= link_to "map votes", root_path, class: "brand" %>

        <ul class ="nav pull-right">
          <li><%= link_to "Active",  active_elections_path %></li>
          <li><%= link_to "Map List", maps_path %></li>
          <% if current_user %>
            <li>Welcome <%= current_user.nickname %></li>
            <li><%= link_to "Sign Out", signout_path %></li>
            <%= link_to image_tag(current_user.avatar_url, alt: "avatar"), current_user.profile %>
        <%else %>
          <li><%= link_to image_tag("sits_small.png", alt: "Sign in with Steam"), "/auth/steam"%></li>
        <% end %>
      </ul>
    </div>
  </div>
  </header>

  <%= render partial: "shared/flash_messages", flash: flash %>

  <div class="container" >
    <%= yield %>
  </div>

  <footer >
      <div class="container">
        <%= link_to "Powered By Steam", "http://steampowered.com" %>
      </div>
  </footer>
</body>
</html>
