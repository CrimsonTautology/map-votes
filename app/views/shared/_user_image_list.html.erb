<% users.in_groups_of(3, false) do |row_user| %>
  <div class="row">
  <% row_user.each do |user| %>
    <div class="col-lg-1"><%= link_to( image_tag(user.avatar_url, alt: user.nickname, class: "img-rounded user-thumbnail"), user.profile) %></div>
  <% end %>
  </div>
<% end %>
